# Local development launcher

`dev.sh` prefers the workspace’s private Node 24 runtime when available, validates the runtime, and starts the API/Vite development pair. Run `./dev.sh` from any directory; it has no named functions. See [`docs/wiki/dev-launcher.md`](docs/wiki/dev-launcher.md).
