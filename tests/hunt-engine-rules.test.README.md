# Hunt engine rule tests

## 1. Purpose

`tests/hunt-engine-rules.test.ts` covers target validation, round and match budgets, decoy limits, future objective capacity, partial completion, final escape, early suspicion scoring, and duplicate accusation rejection.

## 2. Tests

The plan budget test starts at line `59`. Partial target scoring starts at line `114`. Completed objective and final-pair scoring starts at line `130`.
