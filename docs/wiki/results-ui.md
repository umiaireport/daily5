# `web/components/Results.tsx`

`Results` (`web/components/Results.tsx:6`) renders the reveal, token identities, prices, outcome sparklines, round/session returns, benchmarks, and next actions for synthetic or live replay mode. Its nested `share` (`web/components/Results.tsx:22`) calls `downloadScorecard` and surfaces export errors.
