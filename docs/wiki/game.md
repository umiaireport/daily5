# `server/domain/game.ts`

`Game.createSession` (`server/domain/game.ts:40`) creates a cookie-backed anonymous session. `choices` (`server/domain/game.ts:49`) reads stored choices; `session` (`server/domain/game.ts:57`) returns progress/equity; `scenario` (`server/domain/game.ts:74`) resolves one fixture; `assertCurrent` (`server/domain/game.ts:80`) enforces sequential play; and `round` (`server/domain/game.ts:89`) shapes public state without reveal fields. `next` (`server/domain/game.ts:119`) selects the current round or an unreviewed saved result. `advance` (`server/domain/game.ts:133`) records review in `round_reviews` and advances atomically. `unlock` (`server/domain/game.ts:145`) spends one of two clues in a SQLite transaction. `choose` (`server/domain/game.ts:165`) validates and persists one immutable result. `result` (`server/domain/game.ts:221`) reveals only after lock. `leaderboard` (`server/domain/game.ts:229`) ranks completed sessions. `GameError` (`server/domain/game.ts:15`) carries controlled HTTP status.
