# `.prettierrc.json`

The formatter configuration (`.prettierrc.json:1-5`) enables single quotes, a 100-column print width, and trailing commas. `npm run format` writes the configured TypeScript/TSX/CSS/JSON files; `npm run format:check` verifies them without mutation.
