# `server/nansen/client.ts`

`NANSEN_OPERATIONS` (`server/nansen/client.ts:4-10`) allowlists five documentation-derived operations: token discovery, Smart Money netflow, token information, Flow Intelligence, and OHLCV. `ProviderError` (`server/nansen/client.ts:29`) prevents provider body leakage. `stableJson` (`server/nansen/client.ts:77`) makes request hashes stable; `retryDelay` (`server/nansen/client.ts:87`) handles Retry-After/backoff; `createNansenClient` (`server/nansen/client.ts:102`) creates isolated state. Its nested `acquire` (`server/nansen/client.ts:130`), `release` (`server/nansen/client.ts:138`), and `request`/task (`server/nansen/client.ts:144`) implement concurrency/rate spacing, cache/budget/retry, and one fetch/validation task. Returned `usage` (`server/nansen/client.ts:282`) copies counters. This process-local transport remains disabled unless live mode and an explicit key enable it; no synthetic game request invokes it.
