# `fixtures/synthetic/scenarios.ts`

`makeSeries` (`fixtures/synthetic/scenarios.ts:65`) creates a deterministic 12-point series. `SCENARIOS` (`fixtures/synthetic/scenarios.ts:77`) maps five fictional stories to rounds; its nested `clue` factory (`fixtures/synthetic/scenarios.ts:103`) creates typed evidence cards. Fixture outputs include hidden identities, fictional prices, pre-decision series, outcomes, warnings, and cutoff timestamps.
