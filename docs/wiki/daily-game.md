# `server/domain/daily.ts`

`canonical` (`server/domain/daily.ts:62`) creates stable finite-JSON representations for challenge evidence and settlement. `DailyGame` (`server/domain/daily.ts:77`) is a clock-injectable challenge boundary: `now` (`server/domain/daily.ts:83`) validates time, `row` (`server/domain/daily.ts:89`) reads stored state, `assertSupportedRules` (`server/domain/daily.ts:97`) enforces the supported scoring version, `publish` (`server/domain/daily.ts:113`) pins an ordered challenge, `challenge` (`server/domain/daily.ts:161`) exposes only its public payload, `status` (`server/domain/daily.ts:165`) returns lifecycle state without settlement data, `enter` (`server/domain/daily.ts:170`) locks one allocation, `result` (`server/domain/daily.ts:196`) reads its saved state, and `settle` (`server/domain/daily.ts:208`) freezes a complete result or voids after the deadline. `server/app.ts` registers the public summary, entry, and result routes; `server/jobs/worker.ts` supplies private post-window settlement.
