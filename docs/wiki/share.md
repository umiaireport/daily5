# `web/share.ts`

`downloadScorecard` (`web/share.ts:4`) builds a 1200×630 canvas containing synthetic-mode labels, score, virtual equity, benchmarks, and disclaimers; it converts the canvas to PNG, triggers a local download, and revokes the object URL. No upload occurs.
