# `web/styles.css`

This stylesheet defines the dark arcade/ocean visual system, token colors, cards, dialogs, controls, result/leaderboard states, keyboard focus, and responsive breakpoints for the `1440x900` desktop and `390x844` mobile targets. It contains CSS only and no functions.
