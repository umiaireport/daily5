# Browser entrypoint

## Purpose and control flow

`web/main.tsx` imports the app stylesheet and mounts `App` into the `#root` element with React 19.

There are no named functions; top-level mounting is at `web/main.tsx:5`.
