# Modal component

## Purpose and functions

`web/components/Modal.tsx` wraps the native `dialog` element as an accessible modal with Escape/backdrop close behavior.

- `Modal` — `web/components/Modal.tsx:4`.
