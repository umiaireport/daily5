# Vite configuration

`vite.config.ts` enables React, proxies local API calls from browser `:8311` to Fastify `:8411`, emits `dist`, and denies private fixture/server/test/data/runtime/backup paths from Vite’s file server. Its configuration is documented in [`docs/wiki/vite-config.md`](docs/wiki/vite-config.md).
