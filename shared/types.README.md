# Shared game contracts

## Purpose and sections

`shared/types.ts` defines the TypeScript contracts shared by the Fastify domain and React UI: clue kinds, clues, assets, public rounds, result reveals, sessions, and leaderboard entries.

## Functions

This module exports types only and has no runtime functions. Contracts are declared at `shared/types.ts:1-9`.
