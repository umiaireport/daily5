# Playwright configuration

`playwright.config.ts` runs the browser journeys in one worker against local Chromium at desktop and mobile viewports and starts the development services when needed. See [`docs/wiki/playwright-config.md`](docs/wiki/playwright-config.md).
